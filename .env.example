# Environment: "dev" or "prod"
REFLEX_ENV=dev

# Dev ports (only used when REFLEX_ENV=dev)
FRONTEND_PORT=3000
BACKEND_PORT=8000

# Prod port (only used when REFLEX_ENV=prod, single-port mode)
PORT=2009

# App settings
LOG_LEVEL=INFO
USERS=admin:changeme,analyst:changeme
OPENAI_API_KEY=your-openai-api-key-here
